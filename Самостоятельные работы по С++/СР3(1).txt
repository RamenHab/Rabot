//СР 3(1)
//Проверка возможности построения треугольника 
#include <iostream>
#include<math.h>
using namespace std;
int main() 
{
    //float задает переменные
    float a,b,c,s;
    //Дается возможность ввода данных для переменных a,b,c
    cout « "\nВведите значение a="; cin » a;
    cout « "\nВведите значение b="; cin » b;
    cout « "\nВведите значение c="; cin » c;
    //Вычисление площади
    s=a+b+c;
    //Происходит проверка условия if, проверка построения треугольника. Завершение программы.
   if (s>=15)
    cout « "\n Построить можно, s=" « s;
    else
    cout « "\n Построить нельзя, s=" « s;
    return 0;
}